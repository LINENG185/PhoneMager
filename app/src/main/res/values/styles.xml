<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextView">
        <item name="android:background">#ff009acd</item>
        <item name="android:gravity">center</item>
        <item name="android:text">@string/main_name</item>
        <item name="android:textColor">@color/main_name</item>
        <item name="android:textSize">@dimen/main_name_textsize</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="home">
        <item name="android:background">@drawable/selector_home</item>
        <item name="android:clickable">true</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
